# annotation-code-locator
description = "原型标注代码定位器。接收已分组的标注候选，沿真实交互检查主页面、页签、抽屉、弹窗和自定义浮层，为每项选择唯一且稳定的常驻 DOM 宿主，输出 class 注入清单；自定义浮层同时给出 data-annotation-container/content 接入方式，不依赖 UI 库、不修改业务逻辑。"
sandbox_mode = "workspace-write"
